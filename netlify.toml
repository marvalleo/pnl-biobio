[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  # Las variables de Supabase se gestionan directamente en el panel de Netlify

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
